# trading-lab — 로드맵

> 목적지: **로그인해서 쓰는 개인 투자 지휘소.** 트레이딩 봇을 만들고, 과거 데이터로 검증하고,
> 실제로 굴리면서 성과를 비교한다. 각자 자기 컴퓨터에서 자기 계좌로.
> 지금 무엇이 되는지는 [README.md](README.md)에 있고, 이 문서는 무엇이 어떤 순서로 오는지다.

## 지금 — 터미널 골조

화면과 데이터의 뼈대를 세우는 중이다. 진행 상태의 원본은
[마일스톤 「M2 터미널 골조」](https://github.com/Danwoo/trading-lab/milestone/2)이고,
여기에 손으로 복제하지 않는다.

- **셸 개편** — 실험대(홈) · 아이콘 레일 · 보드 옆에 붙는 사이드 패널. 관리 화면은 `/admin` 으로 격리
- **디자인 시스템 적용** — 색·타이포·선·상태를 토큰으로 통일
- **시세 적재 파이프라인** — 실제 소스에서 캔들을 받아, 차트가 적재된 값으로 그려진다
- **워크스페이스 전환** — 멀티테넌시를 개인 워크스페이스(전략별 격리·읽기전용 게스트 초대·봇 신원·레이아웃 저장)로 재해석해 전 계층에 반영

**백테스트는 왔다.** 격자를 돌리면 조합마다 곡선·낙폭·거래 목록·판정 지표가 남고, 그 성과가
어떤 비용 가정 위에 선 것인지 화면이 함께 적는다. 남은 자리(장중 실행·주문)는 여전히 빈 채이고,
빈 자리가 무엇이 올 자리인지 화면이 말한다.

## 다음

- **봇 실행 엔진** — 장중에 신호를 판정하고 거래 로그를 남긴다. 결정론적으로 돈다(LLM 호출 0회) —
  LLM 은 전략 생성·결과 해석 등 원할 때만 쓴다
- **검증의 현실성** — 호가 단위·가격제한폭·유동성까지 반영한 체결. 지금은 명시 비용 3종까지다
- **모의계좌 주문** — 모의·실계좌가 같은 API 인 증권사 모의계좌에서 주문 로직을 개발한다
- **스크리너**

## 나중

- **실주문 승격** — 백테스트·검증을 통과한 전략만
- **호스팅 모드** — 로컬판이 완성된 뒤. 호스팅에서는 도구를 제한하고 실주문을 비활성화한다
- 분봉 확대 · 에이전트 답변 품질

## 데이터 원칙

- **배포판에 시세 데이터는 0바이트** — 이 저장소는 어댑터만 배포하고, 사용자가 자기 API 키로
  조회한다(일부 데이터 소스 약관이 제3자 제공을 금지한다). 키가 없으면 MCP 서버는 MOCK
  금융 데이터로 동작한다.
- **화면의 모든 숫자가 출처를 갖는다** — 실데이터가 있는 패널은 반드시 실데이터, 소스가 없는
  패널은 임시 데이터임을 화면에 표시한다.

## 하지 않는 것 (지금은)

- **실주문 자동화** — 검증을 거치지 않은 전략으로 실계좌를 굴리지 않는다
- **호스팅 SaaS 운영** — 이 저장소의 1차 배포 형태는 각자 자기 컴퓨터에 설치하는 로컬판이다
