<!-- 목표 문안은 초안 — 리드가 확정한다 -->

# CONTEXT.md — 목표층 조종석

> 이 문서의 `#N` 은 대부분 **옛 비공개 레포(`fintech-ai-platform`) 이슈 번호**이며 이 레포에서 열리지 않는다.
> 규약 정본은 [`CLAUDE.md` 「이슈 번호 표기」](CLAUDE.md#이슈-번호-표기).

## 비전

<!-- 거의 바꾸지 않는다 — 바꾸면 결정 로그에도 남긴다 -->

**로그인해서 쓰는 개인 투자 지휘소.** 트레이딩 봇을 만들고, 과거 데이터로 검증하고, 실제로 굴리면서 성과를 비교한다.
전략은 파일로 남아 되짚을 수 있고, 차트·포지션·지표·근거가 한 화면에서 맞물린다.
LLM 은 **내가 원할 때** 옆에서 돕는다 — 전략을 만들어주고, 결과를 해석하고, 다음 판단의 재료를 정리한다.
오픈소스로 배포해 **각자 자기 컴퓨터에서 자기 계좌로** 굴린다.

## 목표 (이번 분기)

<!-- 분기마다 재점검한다 -->

- **G1 — 남이 받아서 자기 컴퓨터에 띄울 수 있다.** 판정: 레포를 처음 클론한 사람이 문서만 보고 기동해 자기 화면을 띄운다.
- **G2 — 화면의 모든 숫자가 출처를 갖는다.** 판정: 임시 데이터인 패널은 화면에 그렇게 표시되고, 나머지는 전부 실제 소스에서 온다.

## 로드맵 — Now / Next / Later

<!-- 베팅 승격·강등 시 갱신한다 -->

- **Now**: **굳히기** — Cycle 5 가 찾은 막힘 3건([#397](https://github.com/Danwoo/trading-lab/issues/397)·[#398](https://github.com/Danwoo/trading-lab/issues/398)·[#399](https://github.com/Danwoo/trading-lab/issues/399))과 다듬기 묶음([#407](https://github.com/Danwoo/trading-lab/issues/407)). 어느 베팅을 골라도 필요한 것들이라 먼저 닫는다.
- **Next**: **M4 보드를 믿는다** — M3 는 「보드가 찬다」였고 이 베팅은 **찬 보드를 검산 가능하게** 만든다. 실사용에서 값이 틀리는 자리가 여섯 나왔다(#400~#405).
- **Next 2**: **M5 봇을 굴린다** — M3 의 no-go 였던 실행 엔진. 지금 화면은 봇이 「켜짐」이라 적는데 굴리는 것이 없다(#406). 믿을 수 없는 백테스트 위에 실행을 얹지 않으려고 M4 뒤에 둔다.
- **Later**: **M6 리서치를 제품 기둥으로** — `/admin` 에 숨은 투자리서치 챗을 두 기둥 중 하나로. 완료 판정을 착수 전에 리드와 문장으로 못박는다.


## 결정 로그

<!-- 추가 전용 — 기존 항목은 수정하지 않는다. 형식: `- 날짜 결정 (기각 대안 — 이유, 감수)` -->

- 2026-07-24 벡터DB=pgvector (Milvus 기각 — 로컬 미배포·무거움 / Supabase 호환 획득 / 하이브리드 검색 일부 보류 감수)
- 2026-07-25 데이터=재시드 (덤프 이행 기각 — 지킬 실데이터 없음)
- 2026-07-25 테이블명=소문자 snake_case (혼합케이스 기각 — Postgres 폴딩 불일치 실증)
- 2026-07-26 드라이버=sync psycopg3 (async 전환 기각 — churn 최소)
- 2026-07-27 DB 1개+스키마 2개 (서비스별 DB 분리 기각 — Supabase 는 프로젝트당 DB 1개 / prisma db push 가 같은 스키마의 남의 테이블을 지우는 것 실증)
- 2026-07-27 doc-search 로컬 벡터DB=fintech DB (별도 docsearch DB 기각 — DB 1개 원칙 정합, force-reset 위험은 #187 로 해소, workspace_doc_chunk 는 doc-search 자가 DDL 소유)
- 2026-07-27 데모 서빙=무료 상용 API — LLM Groq(라우터 openai/gpt-oss-120b·생성 llama-3.3-70b-versatile)+임베딩 Jina v3 1024차원 (CPU 로컬 서빙 기각 — 품질·속도, GPU 부재 / 감수: 업로드 문서·질의가 외부(Jina·Groq)로 전송 — 데모엔 샘플 문서만)
- 2026-07-28 제품=로그인형 개인 투자 지휘소, 오픈소스 로컬 배포판 우선 (호스팅 SaaS 선행 기각 — 로컬이 완성된 뒤 확장 / 멀티테넌시는 제거하지 않고 유지)
- 2026-07-28 회사=개인 워크스페이스로 재해석 (멀티테넌시 제거 기각 — 전략별 격리·읽기전용 게스트 초대·봇 신원·레이아웃 저장으로 재사용 / 관리 화면은 제품 메뉴에서 빼 관리자 경로로)
- 2026-07-28 봇=여러 전략의 조합 (봇 1개=전략 1개 기각 — 퀀트 봇은 복합 전략 / 전략은 파일·봇은 화면에서 찍어내는 하이브리드, 전략이 자기 캔들 주기를 선언해 멀티 타임프레임 성립)
- 2026-07-28 시간축 분리 — 장중 봇은 LLM 0회, 저녁 8시 연구 배치에서만 LLM (장중 실시간 LLM 기각 — 무료 한도가 오전에 소진되고 비용 예측 불가 / 봇은 결정론적이라 장중 상시 실행이 공짜)
- 2026-07-28 실주문 보류 (백테스트·검증 완료 후 승격 / 주문 로직은 모의·실계좌가 같은 API 인 KIS 모의계좌에서 개발)
- 2026-07-28 데이터 소스 = 국내 KRX 오픈API · 미국 Twelve Data · 주문 KIS · 토스증권은 2차 시세 (yfinance 기각 — ToS 가 자동수집을 명시적으로 금지 / 토스 단독 기각 — 모의투자 부재로 첫 실행이 실거래)
- 2026-07-28 데이터 API 키=워크스페이스 설정 (전역 설정 기각 — KRX 등이 비상업·제3자 제공 금지라 사용자가 자기 키로 조회해야 함)
- 2026-08-09 제품 정체성 전파 — 실험대(봇)와 리서치는 **나란한 두 기둥**, 시간축으로 분리 (리서치를 곁다리로 강등 기각 — 12 sub-agent·MCP 6서버가 이미 서 있고 「봇이 모르는 것」을 올리는 역할이 명확 / README·ROADMAP·CLAUDE.md 가 07-28 결정 이전 문안이었던 것을 이번에 맞춤)
- 2026-08-09 시스템이 거는 금액 상한 제거 — 계산은 판단 재료로만, 결정은 사용자 (상한식 유지 기각 — 손실 경험 페르소나가 슬라이더·대기·봇 추가로 사흘 만에 전부 우회 / 남기는 선은 평온할 때 본인이 적은 손실 한계선 하나)
- 2026-08-09 셸=실험대가 홈, 터미널은 해체해 「근거」로 분산 (터미널을 패널에 통째 삽입 기각 — 창 안의 창, 크롬 두 겹 재현)
- 2026-07-28 UI 라이브러리=오픈소스 조합(그리드 · lightweight-charts · shadcn/ui) (DevExtreme 유지·Syncfusion Community 기각 — 오픈소스 배포와 상용 라이선스가 모순 / 평가판 배너 제거는 갈아타는 길뿐, React 19 라 다운그레이드 불가 실측)
- 2026-07-28 구조=모듈러 모놀리스 + MCP 서버 유지 (서비스별 프로세스 기각 — 개인 설치판에서 16 프로세스는 진입장벽 / 레이어·네이밍·anti-patterns 규율은 그대로 유지, MCP 는 프로토콜 정체성이라 분리 유지)
- 2026-07-28 배포 모드 분리 — 로컬(Claude Code 임베드·실주문 가능) / 호스팅(도구 제한 콘솔·실주문 비활성) (단일 모드 기각 — 호스팅에서 셸 권한은 테넌트 격리를 무력화)
- 2026-07-28 LLM 권한=승인 후 반영, 자동은 가역적인 것만 (전면 자동 기각 — 봇 정지·알림·리포트는 자동, 설정 변경·주문은 승인, 삭제는 자동 금지)
- 2026-07-28 화면=트레이딩 터미널 중심 + 자유 배치 패널 (관리 화면 나열 기각 — 종목 선택이 전 패널에 브로드캐스트되는 한 판이 실제 사용 방식)
- 2026-07-28 임시 데이터 규칙 — 실데이터가 있는 패널은 반드시 실데이터, 소스 미확보 패널은 후순위로 두되 골조는 만들고 임시 데이터임을 화면에 표시 (전면 mock 금지 기각 — 껍데기를 먼저 짓는 순서와 충돌 / 실데이터 연결이 그 패널의 완료 조건)
- 2026-07-28 데이터 소스 보정 — 국내 **data.go.kr 금융위 API**(KRX 오픈API 기각 — 제3자 제공 금지 조항이 오픈소스 배포와 충돌) / 미국은 **재선정 필요**(Twelve Data 기각 — 무료 티어가 외부 표시를 금지) / 미국 재무·종목마스터·섹터·13F·ETF 흐름은 SEC·FINRA·OpenFIGI 로 **인증 없이** 확보 (근거 #230)
- 2026-07-28 MSA 스타터 템플릿을 별도 레포로 분리 (한 레포에 템플릿·제품 공존 기각 — 제품에 맞춰 뜯으면 템플릿이 아니게 됨 / 분리 직전 상태는 태그 `msa-template-2026-07`)
- 2026-07-29 미국 시세=Alpaca(사용자 자기 키) · 배포판에 데이터 0바이트 (합성 데이터 기각 — 실전 투자용이라 가짜 캔들 백테스트는 결과가 무의미 / yfinance 기본 탑재 기각 — Yahoo 약관이 personal use only 이고 pandas-datareader 가 정확히 이 축에서 증권 리더를 전면 삭제 / 오픈소스 선례가 예외 없이 "어댑터만 배포, 키는 사용자" 로 수렴 — 근거 #240)
- 2026-07-29 시세·종목마스터는 **전역 공용**, 워크스페이스 스코프는 API 키뿐 (워크스페이스별 저장 기각 — 같은 값이 워크스페이스 수만큼 복제되고 적재가 반복되어 소스 한도를 곱절로 소모 / 키만 워크스페이스별인 이유는 약관 / 호스팅 모드에서는 성립하지 않아 별도 판단 선행)
- 2026-07-29 데이터 소스 API 키=암호화 저장 (평문 기각 — 새 민감정보 카테고리이고 평문 선행 후 암호화는 비용이 훨씬 큼)
- 2026-07-29 가입 시 개인 워크스페이스 자동 생성 (도메인 매칭 단독 기각 — 개인 지휘소는 가입 즉시 자기 공간이 있어야 성립)
- 2026-07-29 터미널이 메인 화면 — 기존 MDI 탭 밖 (탭 안 배치 기각 — 이 제품에서 터미널이 곁다리 화면일 수 없음 / 감수: 앱 껍데기 개편, 대신 탭 iframe 으로 인한 문맥 공유 불가 문제가 함께 해소)
- 2026-07-29 종목 사이드바를 M2 에 포함 (P2 이연 기각 — 없으면 종목 전환 UI 가 없어 사용자 스토리 1 이 사람 눈으로 완결되지 않음)
- 2026-07-29 UI 조합 = AG Grid Community(표) · lightweight-charts(캔들) · ECharts(지표) · react-grid-layout 2.x(타일 배치) — 전부 MIT/Apache-2.0 (도킹 패널 기각 — Tableau·Power BI 식 타일 그리드가 목표 / TanStack 기각 — 필터·페이징·리사이즈를 직접 만드는 유지보수 부담 / AG Grid Enterprise 기능은 쓰지 않는다)
- 2026-07-29 서버 상태 캐시(TanStack Query) 도입 (미도입 기각 — 패널 여럿이 종목 전환마다 요청을 날리는 구조라 중복 제거·취소·캐시가 나중이 아니라 지금 필요)
- 2026-07-29 통합 앱 폴더명 변경 — 서비스 통합과 같은 작업에서 (유지 기각 — 어차피 통합으로 폴더가 재편되므로 두 번 건드리지 않는다)
- 2026-07-29 멤버십 사용자 키=ID 기반 (email 자연키 기각 — 이메일 변경 시 권한이 끊김 #238)
- 2026-07-29 화면 배치=브라우저 저장 · 종목은 주소창에 싣지 않음 · 패널 수 상한 없음(요청 상한으로 제어) · 차트 출처 로고 표시 유지(라이선스 준수)
- 2026-07-29 남은 화면의 라이브러리 교체=M2 직후 (오픈소스 공개 직전 기각 — 두 스택 공존 기간을 줄인다)
- 2026-07-29 배당·액면분할 반영=P2, 단 설계는 M2 에서 (M2 구현 기각 — 소스 미확정이고 적용할 소비자(백테스트)가 있어야 검증됨 / 무수정 원본 저장 + 정책 기록까지는 M2)
- 2026-07-30 표시 타임존=사용자 타임존 — 공용 포맷터 `formatDate` 의 date·datetime·time 세 모드가 모두 런타임 기본 타임존으로 렌더한다 (KST 고정 기각 — 각자 자기 컴퓨터에서 굴리는 오픈소스 배포라 표시 시각이 사용자 시계와 맞아야 하고 미국 시장이 범위 안 / **경계: 시세 차트 시간축은 대상이 아니다** — 캔들은 관례상 시장 시각 고정이므로 차트는 이 기본값을 상속하지 말고 시장 타임존을 명시하거나 차트 전용 포맷터를 쓴다 / 감수: 서버가 문자열로 찍어 보내는 관리 화면 타임스탬프는 쓰기·읽기가 함께 걸린 별개 경로라 이 결정만으로 바뀌지 않는다 — #263, 후속 필요)
- 2026-08-02 라이선스=MIT (AGPL 기각 — 받는 사람이 서비스로 제공할 때 소스 공개 의무를 지우는 제약을 걸지 않는다, #287)
- 2026-08-02 통합 앱 폴더명=`backend-service` 유지 (2026-07-29 "변경한다" 결정을 재확정으로 뒤집음 — 참조 파일 49개의 전수 갱신 위험·비용이 개명 이득보다 크다, #291)
- 2026-08-02 가드레일 타임아웃·호출 실패 시=차단(fail-closed) (기존 fail-open 기각 — #266 셀프 가입 개방 + #207 LLM 무료 한도 공유가 겹쳐 "한도가 차면 가드레일부터 죽고 그 순간이 문이 열리는 때"가 실재 위험이 됨 / 감수: 가드레일 프로바이더 장애가 채팅 전체 장애가 된다, #338)
- 2026-08-03 공개 배포=별도 공개 레포에 **현재 트리만** 내보낸다, 이 레포는 비공개 유지 (히스토리 재작성+force push 기각 — 모든 커밋 해시가 바뀌어 열린 PR·클론·이슈 참조가 깨진다 / 현행 유지 기각 — 출처 불명 제3자 이미지가 히스토리에 남아 MIT 로 재배포된다 / 감수: 공개본에 개발 이력이 남지 않는다, #360)
- 2026-08-05 공개 레포 이름=`trading-lab`, 공개본에서 제외하는 것 없음 — `.docs/`·`.claude/`·`CONTEXT.md`·`AGENTS.md`·CI 워크플로 전부 포함 (선별 제외 기각 — 무엇을 뺐는지가 또 하나의 관리 대상이 된다 / 감수: 하네스 자동화 워크플로가 공개 레포에서 실패하거나 무동작한다, Actions 설정은 공개 시점에 사람이 정한다, #360)
- 2026-08-05 두 번째 이후 릴리스=커밋 1개씩 **append**, force push 금지 (매번 히스토리 갈아엎기 기각 — 공개본을 클론한 사람의 `git pull` 이 깨지고 포크·PR·이슈의 커밋 참조가 무효가 된다 / 선례: google/copybara SQUASH 모드 / 절차·근거: [`.docs/6-도구/공개배포-릴리스.md`](.docs/6-도구/공개배포-릴리스.md), 실행: `scripts/release_public.py`, #360)
- 2026-08-06 CI 체크 = 결정론적인 것만 (AI 리뷰·계획 경고·정보성 잡을 체크에서 제외 — Orca 는 failed·pending 이 하나라도 있으면 머지를 막으므로, 「코드가 맞으면 반드시 초록」을 못 지키는 체크는 작업 정지 장치가 된다. 체크 59→18·잡 52→11, 단언은 하나도 안 버림. 감수: 리뷰 실패가 체크로는 안 보여 코멘트·라벨·요약으로만 드러난다)
- 2026-08-06 CI 리뷰어 기동 = `orca worktree create --agent <id>` (bare create 후 `terminal create --command` 기각 — Orca 문서가 명시한 안티패턴이고, 셸이 터미널 제목을 덮어써 「어느 것이 에이전트인가」 추측이 구조적으로 실패한다. 핸들은 `agentTerminalHandle`/`startupTerminal.handle` 이 유일한 출처)
- 2026-08-06 리뷰 실패 시 워크트리 보존 (즉시 정리 기각 — 실패의 현장을 사람이 봐야 한다. 다음 실행의 시작 스윕이 회수하므로 무한 누적은 없다)
- 2026-08-04 공개 배포=히스토리 없이 새 레포 (히스토리 재작성 기각 — 전 커밋 해시가 바뀌어 열린 PR·클론·이슈 참조가 깨진다 / 현행 유지 기각 — 제3자 자산이 히스토리째 재배포된다 / 감수: 개발 이력이 공개본에 남지 않는다, #360)
- 2026-08-07 데이터 소스 API 키=`.env` 전역 설정, 저장 암호화 없음 — **2026-07-29 의 두 결정("데이터 API 키=워크스페이스 설정"·"데이터 소스 API 키=암호화 저장")을 뒤집는다.** 근거는 제품 정의 변경이다: 2026-07-28 결정으로 이 제품은 오픈소스 로컬 배포판 우선이고 「각자 자기 컴퓨터에서 자기 계좌로」 굴리므로 1인 설치에서 워크스페이스별 키는 의미가 없고 `.env` 가 그 배포 형태의 관용구다 (워크스페이스별 암호화 저장소 기각 — 짓지 않는다 / 저장 암호화 기각 — `.env` 값을 다시 암호화하면 복호화 키를 둘 자리가 결국 같은 `.env` 라 자물쇠와 열쇠를 한 상자에 넣는 것이고 보호가 늘지 않는다. 방어는 파일 권한·gitignore + 유출 4축(로그·응답·에러·커밋) 그물로 옮긴다 / **감수: 호스팅 모드에서 성립하지 않는다** — 여러 사람이 한 인스턴스를 쓰면 한 사람의 키로 전원이 조회하게 되고, 그것은 원 결정의 근거였던 「data.go.kr·KRX 등이 비상업·제3자 제공 금지」와 정면 충돌한다. **호스팅 모드를 열 때 이 결정을 다시 판단한다**, #2)

- 2026-08-08 리뷰 워크트리 회수=**사람** — 나이(`lastActivityAt`)로 생사를 판정하던 고아 스윕을 삭제한다. **2026-08-06「다음 실행의 시작 스윕이 회수하므로 무한 누적은 없다」의 뒷절을 이것이 대체한다** (그 결정의 앞절 「실패 시 워크트리 보존」은 그대로 유효하다). 근거: Orca 가 주는 `lastActivityAt` 이 생성 시각에 고정돼 갱신되지 않아 안전 조항이 무력했고, 그 스윕이 **일하는 중인 리뷰어 다섯을 회수**했다(후보 5건 검사·회수 5건·보존 0건, 그중 하나는 6분 전 지시를 받은 워커인데 「마지막 활동 52360초 전」으로 읽혔다). 나이 기준 회수를 고쳐 쓰는 것 기각 — 그 값은 Orca 소관이라 우리가 못 고친다 / **감수: 실패한 리뷰의 `review-*` 워크트리가 쌓이고 사람이 치운다** (`orca-ide worktree list` → `worktree rm --worktree name:<이름> --force`). 연결 PR 상태로 판정하는 회수기는 automation 계획 Task 4 소관이고, 그때까지의 공백은 사람이 메운다. 이슈 [#23](https://github.com/Danwoo/trading-lab/issues/23))

- 2026-08-07 이사 모델=**완전 이사** — 공개 레포(`trading-lab`)로 개발 자리를 옮기고 거기서 개발을 계속한다, 비공개 레포는 보관용. **2026-08-03「이 레포는 비공개 유지」와 2026-08-05「두 번째 이후 릴리스=append」가 전제한 상시 릴리스 모델을 이것이 대체한다** (「비공개 개발 + 공개 릴리스 미러」 기각 — 릴리스마다 공개 워킹트리를 개발 트리로 덮으므로 공개 레포에 온 기여가 다음 릴리스에 조용히 사라지고, 이력·이슈·PR 이 두 레포로 갈려 어느 쪽이 정본인지가 매번 판단거리가 된다 / `release_public.py` 는 **첫 이사 1회용 도구**가 되고 그 뒤 「릴리스」라는 개념은 없어진다 — 변경은 공개 레포의 보통 커밋·PR 이다 / **감수: 앞으로의 개발 히스토리·실패한 시도·논의가 전부 공개된다**, 공개된 것은 회수할 수 없다, #360)
- 2026-08-09 dependabot 자동 머지 기준=**major 만 사람** — 근거가 무너진 것을 알고 유지한다. 이 정책을 만들게 한 사례 둘 중 `pyjwt 2.12.1 → 2.13.0` 은 **minor** 라 정책 밖으로 샌다(초안이 「둘 다 major」로 잘못 적었다). 그 사실을 대고 다시 물었고 리드가 그대로를 골랐다 — 「사람 부담을 최소화하고 중요한 의사결정에 대해서만」 (㉡ minor 도 사람 기각 — 자동 비율이 크게 줄어 부담이 늚 / ㉢ 리뷰 판정 내용으로 가름 기각 — 가장 정확하나 마커에 신호를 실어야 해 구현이 늚 / **감수: minor 의 동작 변경이 아무도 안 읽은 채 흐른다**. 그 비용이 실제로 물렸을 때 ㉢ 을 다시 상정한다, #23)
- 2026-08-09 위험도 출처=**`Refs #N` 도 읽는다** — `closingIssuesReferences`(closing 키워드만 잡음) 에만 기대면 위험도를 읽히려고 PR 마다 전용 이슈를 만들게 되고 이슈가 문어발로 는다. 이슈 하나에 PR 여럿을 매달 수 있어야 한다 (`Closes` 전용 기각 — 이슈 증식 / **여럿 참조 시 최고 위험을 취하고 하나도 못 읽으면 미선언=고위험**, fail-closed / 감수: `Refs` 는 「관련」이지 「닫음」이 아니라 그 이슈의 위험도가 그 PR 것이 아닐 수 있다, #23)
- 2026-08-09 required 게이트=**`test: gate` 하나가 `test: ` 접두 체크런 전수를 대표** — 개별 잡을 required 로 걸면 pending 창이 여럿이 되고, `needs:` 는 같은 워크플로 안만 묶어 당시 흩어져 있던 다른 워크플로의 10종을 대표하지 못한다 (개별 required 기각 / `needs:` 기각 / required 항목은 GitHub Actions 앱 `15368` 에 고정 — 고정이 없으면 같은 이름을 보고하는 다른 앱도 만족시킨다). **체감 실측**: 게이트가 더 잡아먹는 시간은 상류 마지막 대비 **4~13초**이고, 문서 전용 PR 은 **22초**에 초록이다(8종 skipped). 코드 PR 의 2~3분은 backend·frontend 테스트가 원래 걸리던 시간으로, 전에는 그것이 도는 중에 버튼이 열렸다 — 늘어난 시간은 게이트의 비용이 아니라 **원래 안 보고 머지하던 것을 보게 된 값**이다 (감수: 브랜치가 오래돼 게이트 잡을 못 가진 PR 은 그 체크가 **목록에 아예 안 나타나** 왜 막혔는지 읽기 어렵다 — `gh pr update-branch` 로 푼다, #23)
- 2026-08-09 에이전트 세션 격리=**세션마다 Orca 워크트리** — 리드의 원본 체크아웃은 오리엔테이션 전용(읽기)으로 둔다. 근거는 실사고다: 세션 둘이 `~/projects/trading-lab` 한 디렉터리에서 각자 `git checkout` 을 해 **`HEAD` 가 하나**였고, 한 세션이 자기 브랜치에 있다고 믿고 커밋한 것이 남의 브랜치 위에 앉아 그 diff 가 문서 3종(1,099줄)을 삭제하는 모양이 됐다. 상대는 자기 파일이 사라진 것을 삭제 사고로 오진했다 (별도 클론 기각 — Orca Projects 패널에 안 떠 리드가 워커를 직접 들여다볼 수 없다 / 규율로만 가르기 기각 — 격리가 아니다 / **감수: `refs/stash`·`.git/shallow` 는 계속 공유한다** — stash 는 안 쓰기로 이미 정했고 `fetch --unshallow`·`gc` 는 클론에서만 한다. 이번에 원격이 무사한 것은 브랜치가 우연히 갈라져 push 가 거부된 덕이지 규율 덕이 아니다)

- 2026-08-09 M2 완료 판정=「시세」 화면(현행 터미널)까지, **셸 개편은 M3** (판정을 실험대 보드로 옮기는 것 기각 — 보드에 상시로 있는 넷 중 격자·곡선·내 봇이 전부 백테스트·봇 엔진 산출물이라 M2 의 no-go 와 정면 충돌한다, PR #68 본문도 「백테스트 엔진이 없어 실측을 못 뽑았다」고 적는다 / no-go 에서 봇 엔진·백테스트를 빼는 것 기각 — M2 가 마일스톤 하나가 아니라 제품 절반이 된다 / 옛 판정의 「배치 변경 후 새로고침 유지」는 **삭제** — 2026-08-09 셸 결정이 자유 배치를 없애 지킬 대상이 사라졌다 / **감수: M2 를 통과해도 로그인한 사람 눈에 08-09 셸은 안 보인다** — 실험대 홈·46px 레일·372px 패널이 전부 M3 이고, 자유 배치 코드(`PanelGrid`·`react-grid-layout`·`layoutStore.grid`·`gridMath`)는 그때까지 살아 있다)

- 2026-08-09 **셸 개편·디자인 시스템 적용을 M2 안으로 되돌린다 — 같은 날 앞 항목("M2 완료 판정=「시세」 화면까지, 셸 개편은 M3")을 뒤집는다.** 근거는 그 판정이 리드가 **지금 하고 있는 일**을 다음 마일스톤으로 민 것이었다는 점이다: 2026-08-09 하루의 산출물(시안 37개·결정 30절·디자인 시스템 277줄)이 「나중에 지을 명세」로 읽혔고, 그 위에서 적재 패널이 **없애기로 한 자유 배치 격자**에 등록되고 **폐기될 옛 토큰**(`slate-*`·`ink-primary`·`signal-warn`)으로 그려졌다 (PR #70 — `.docs/4-아키텍처/디자인-시스템.md` 를 열지 않은 채 UI 를 썼다). 보드 내용을 M2 에 넣는 것은 여전히 기각 — 격자·곡선·내 봇은 백테스트·봇 엔진 산출물이라 no-go 와 부딪힌다. 대신 **셸과 빈 상태까지**를 M2 로 하며, 그 자리는 설계가 이미 정해 뒀다(§20.5 첫 진입·§21.4 빈 상태). **감수: M2 가 커진다** — `app/(main)/layout.tsx`·`GlobalTabs`·`tabStore`·권한 게이트·`isEmbed` 가 한 덩어리로 묶여 있고 `shell.html` 이 "회귀 위험이 여기가 가장 크다"고 못박은 자리다. 그리고 **M2 가 PR [#68](https://github.com/Danwoo/trading-lab/pull/68) 에 의존하게 됐다** — 디자인 시스템 문서와 시안 37개가 그 브랜치에만 있다

- 2026-08-09 목표층 문서(`CONTEXT.md`·`CLAUDE.md`·마일스톤) 변경은 **통행료를 걷지 않는다** — 브랜치 이름 고정·PR 본문 3줄·`gate declare` 없음·리뷰 대기 없이 바로 머지, 셋 밖의 파일이 섞이면 면제 소멸(fail-closed). 계기는 리드의 마찰 제기다: 6줄짜리 결정 기록 하나에 브랜치·PR·게이트 선언·리뷰 대기가 붙어 "원래 main 에서 관리하던 것"보다 어색해졌다 (**ruleset 에서 `pull_request` 를 빼는 것 기각** — main 은 `main protection` 으로 보호되고 `bypass_actors` 가 비어 있는데, 에이전트가 리드와 **같은 GitHub 계정으로 push** 하므로 리드에게 여는 문이 에이전트에게도 열린다. 서버측에서 둘을 가를 방법이 지금 없다 / **검사 스크립트를 붙이는 것 기각** — 「표식을 늘리는 것은 방어가 아니다」 / 실측 근거: 문서 전용 PR 은 22초에 초록이라 마찰의 대부분은 CI 가 아니라 그 위에 쌓인 절차였다 / **감수: 규약일 뿐 기계가 안 잡는다** — 면제를 넓게 해석해 코드가 섞인 PR 을 문서 취급하면 아무도 못 막는다)
- 2026-08-09 provenance=**작성·리뷰·머지 셋 다 남긴다** — 트레이드오프로 내지 않는다 (리드: 「누가 작업했는지도 남겨야하고 다 남겨야죠. 사람, 클로드, 코덱스, kimi 누가 작업했는지 어떤 모델이 작업했는지 당연히 남겨야죠」). 머지 커밋에 `작성: … · 리뷰: … · 머지: …` 한 줄을 남겨 `git log` 에서 바로 읽히게 한다 (GitHub 을 열어 PR 로 들어가야 아는 것 기각 — 그건 안 남은 것과 같다 / **순서 강제**: 리뷰어를 실제로 반대 티어로 띄우는 것이 티어를 기록하는 것보다 앞선다 — 종전에는 티어를 계산해 이름표에만 붙이고 `--agent claude` 로 기본 모델을 띄웠으므로 기록하면 **없는 사실을 기록**하는 것이고 그것은 기록 없음보다 나쁘다 / 감수: 모르는 것은 「미상」으로 적는다, 옛 마커·신원 없는 커밋이 그렇다, #23)
- 2026-08-09 머지 주체 구분=**경로로 가른다, 라벨 신고를 폐기한다** — 리드와 지휘자가 같은 GitHub 계정을 써서 액터로 안 갈리는데, **워크플로가 민 머지는 `app/github-actions` 로 기록된다**(실측: #57·#66 워크플로 / #67 사람). 지휘자는 `gh pr merge` 로 직접 밀지 않고 위임 머지 워크플로에 시킨다 (`agent: merged` 라벨 기각 — 깜빡하면 안 남고 안 남은 것과 사람이 누른 것이 같아 보인다. 이 레포엔 그 라벨을 읽는 `merge-record` 잡조차 없어 하루치 신고가 아무 데도 기록되지 않았다 / 별도 계정 기각 — 없다 / 감수: 지휘자가 규약을 어기고 직접 밀면 다시 섞인다, 훅으로 막는다)
- 2026-08-10 문서 전용 PR=**경량 가시화**(㉢) — 리뷰 대상으로 되돌리지는 않되 「리뷰를 안 했다」를 남긴다. `paths-ignore` 로 문서 PR 은 리뷰·마커·기록이 아예 없는데, Task 8 이 `review-gate` 의 「문서 전용 → `human: merge`」 라우팅을 지우면서 가시화 장치가 0이 됐다 (㉠ 수용 기각 — 조용한 것이 통과로 읽힌다, 이 레포가 반복해 데인 클래스다 / ㉡ `paths-ignore` 축소 기각 — 문서 PR 마다 리뷰어를 띄우는 비용 / 감수: 문서 PR 은 여전히 판정을 안 받는다, 가시화는 「안 봤다」를 보이게 할 뿐이다, #23)
- 2026-08-10 고지 문서=**의존성 PR 에서 자동 갱신**(㉠) — CI 가 검사만 하고 갱신은 사람이 하던 것을 워크플로가 대신한다. 실측 근거: 하루에 dependabot PR 4건(#63~#66)이 전부 `THIRD-PARTY-NOTICES.md` 미갱신으로 실패해 지휘자가 넷 다 손으로 생성기를 돌렸고, 8월 8일 #20 에서도 같은 일이 있었다 (㉡ dependabot grouping 기각 — PR 수는 줄지만 원인이 남는다 / ㉢ 사람이 처리 기각 — 반복되는 손일이다 / **함정**: `GITHUB_TOKEN` push 는 새 워크플로 실행을 안 일으켜 고쳐도 CI 가 다시 안 돈다, `PROJECT_TOKEN` 을 쓴다 / 감수: 봇이 봇 PR 브랜치에 커밋한다 — 사람 PR 엔 안 얹는다, #23)
- 2026-08-12 탈퇴자 PII=**식별 데이터는 삭제, 행위 기록은 익명화** — 감사 컬럼 `reg_id`·`mod_id`(26개 테이블)는 탈퇴 트랜잭션에서 `deleted-user-<사용자ID>` 로 치환하고 행은 보존, 기존 잔존분은 alembic `0013`(적용은 사람 몫) (전부 삭제 기각 — 남의 행의 감사 이력이 통째로 빈다 / 보존 기각 — 아무도 판단한 적 없이 남아 있던 PII 가 결정으로 굳는다 / 단일 상수 `deleted-user@…` 로 뭉개기 기각 — 탈퇴자 간 행위 구분이 사라져 감사 이력의 의미가 절반 없어진다 / 감수: 익명화는 비가역이고, NULL 이 아니라 「탈퇴한 사람이 했다」로 남긴다, [#3](https://github.com/Danwoo/trading-lab/issues/3))
- 2026-08-15 대상 사용자=**단계로 가른다** — 지금은 **나**(투자하면서 코드도 다루는 사람, 클론해서 기동), 나중에 일반 투자자로 넓힌다 (둘 중 하나 고르기 기각 — 화면 설계는 「퀀트 처음인 사람」을, G1 은 「클론해서 띄우는 사람」을 대상으로 적고 있어 **서로 다른 사람**이었다 / 튜토리얼(설계 §21.1)은 **나중 단계로 보류**, 지금은 볼 사람이 없다 — `#73` 승인 계획의 가정과 같은 결론 / 감수: 일반 사용자 단계에서 배포 방식부터 다시 봐야 한다)
- 2026-08-15 `--ink-faint` WCAG 미달=**㉡ 채택** — 텍스트용 명도를 3단으로 줄이고 4단째는 **비텍스트(선·아이콘) 전용**으로 강등, 라벨·표 헤더는 `--ink-muted`(4.93:1)가 받는다 (㉠ 4단 유지 기각 — 본문 AA 4.5:1 까지 밝히면 `--ink-muted` 와 간격이 **1.08배**로 좁아져 위계가 사실상 3단이 된다, 전수 계산으로 확인 / 감수: 라벨이 2.74:1 → 4.93:1 로 진해져 화면 밀도 인상이 바뀐다 / 근거와 재현 코드는 `#73` 코멘트)
- 2026-08-15 **봇 만들기를 M2 안으로, 검증은 다음 베팅으로** — 완료 판정에 「대화로 봇 하나를 만들어 저장하고 그 조건이 폼에 그대로 보임」을 더하고 no-go 에서 「봇 만들기」를 뺐다 (봇 전체를 M2 에 넣기 기각 — 백테스트 엔진은 문서가 「별도 스파이크·조사 대상」이라 적을 만큼 크고, 시세 파이프라인이 *"백테스트는 로컬 DB 에서 읽는다 … 적재가 먼저다"* 라고 못박아 **검증은 적재(#2)에 물려 있다** / 반면 **봇을 만드는 것은 거기 안 물린다** — 대화가 폼을 채우고 저장되는 데까지는 시세 없이 성립한다 / 감수: M2 를 끝내도 격자·곡선은 빈 채로 남는다)
- 2026-08-15 봇 만들기 대화의 런타임=**Claude Agent SDK 를 지금 세운다**(㉮) — 새 서비스 `bot-agent-service`(Python), 인증은 서버 프로세스의 `ANTHROPIC_API_KEY` 하나, 도구는 `dontAsk`+최소 allowlist (㉯ M2 는 Client SDK 툴러너로 좁게 가고 검증 베팅에서 갈아타기 기각 — 「되돌리기 비싸다」는 비용이 실제로는 ㉯ 쪽에 붙는다, 툴러너로 시작하면 스트리밍·세션·권한 계층을 우리가 짜고 전략 파일 생성 시점에 통째로 버린다 / Managed Agents 기각 — 도구가 Anthropic 컨테이너에서 도는데 전략 파일은 내 컴퓨터의 레포에 있어야 한다 / **제약: 사용자의 claude.ai 로그인을 태우는 설계는 약관이 막는다** — 승인 없이는 API 키 방식만, 게스트 초대·배포판으로 넓힐 때 다시 봐야 한다 / 프로세스를 가르는 이유는 배포 모드 — 호스팅은 이 서비스를 **안 띄우면** 끝이고, Next 서버 안 라우트는 빼먹으면 셸 권한이 테넌트 격리를 무력화한다 / 감수: M2 범위(대화가 폼을 채운다)는 파일도 셸도 안 써서 Agent SDK 의 값어치가 M2 안에서는 안 드러난다 — 근거는 [#150 코멘트](https://github.com/Danwoo/trading-lab/issues/150#issuecomment-5301722812))
- 2026-08-15 자유 배치 철거 — **2026-07-28 「화면=트레이딩 터미널 중심 + 자유 배치 패널」과 2026-07-29 UI 조합의 `react-grid-layout`(타일 배치) 축을 걷어낸다.** 2026-08-09 셸 결정(§20.2)이 「패널은 레일이 여닫고 보드를 덮지 않는다」로 정한 것을 코드에 반영한 것이라 새 결정이 아니라 집행이다. 같은 날 「M2 완료 판정」 항목이 적은 **감수(「자유 배치 코드는 M3 까지 살아 있다」)는 소멸했다** — 셸 개편이 M2 로 되돌아오면서 그 코드가 M2 안에서 지워졌다. 지운 것: `react-grid-layout` 의존성(전이 포함 11개) · `PanelGrid` · `gridMath` · `layoutStore.grid`/`applyGrid` · `GRID_COLUMNS_COUNT` · 패널 메뉴의 이동 4방향·크기 2축. 살린 것: 출처 배지·에러 경계·스켈레톤·`provenance`·`capabilityMatrix`(G2 가 여기 걸려 있다) (좌표만 남기고 라이브러리만 바꾸는 것 기각 — 없애기로 한 것은 라이브러리가 아니라 **사람이 옮기고 키우는 배치** 자체다 / 옛 저장본 폐기 기각 — 좌표가 사라졌다고 저장본째 버리면 열어 둔 패널 구성이 조용히 없어져, 스키마 v2 마이그레이션이 좌표만 뗀다 / **감수: 「배치 변경 후 새로고침 유지」를 대신할 것이 없다** — 지금 저장되는 것은 「무엇이 열려 있는가」뿐이다, [#73](https://github.com/Danwoo/trading-lab/issues/73) S3)
- 2026-08-18 **다음 베팅은 백테스트 엔진 — 「결과가 화면에 차는 것」이 목표다.** 리드가 QuantDinger([OpenByteInc](https://github.com/OpenByteInc/QuantDinger), Apache-2.0, ★10.8k)를 보고 *"우리가 지향하는 바가 이런 느낌"* 이라 판정했다. **그 화면이 좋아 보이는 이유는 시각 디자인이 아니다** — 저쪽은 평범한 라이트 어드민 테마(Ant Design 계열)이고 우리 디자인 시스템이 더 의도적이다. 좋아 보이는 이유는 **돌아간 결과가 화면에 꽉 차 있다**는 것이다: 자산·승률·손익비·MDD 카드, 날짜별 손익 격자, 드로다운 곡선, 시간대 분포, 캔들 위에 찍힌 매수·매도 마커, 「돌고 있음」이 초록으로 뜨는 전략 목록. 그것이 있으려면 **백테스트 엔진과 봇 실행 엔진**이 있어야 한다 (냉정한 관측: 저쪽 숫자는 장난감 계좌다 — MDD 1,687%·손익비 0.09·자산 $47.57. **화면은 찼지만 뒤의 데이터는 테스트다** — 우리가 베끼는 것은 「채우는 구조」이지 「채운 척」이 아니다). 순서는 **지금 M2 를 끝내고 그다음**이다 (M2 안으로 끌어오기 기각 — 백테스트는 시세 적재(#2)에 물려 있는데 그 키가 아직 없어, 지금 넣으면 mock 위에 성과 화면을 짓게 된다 / 감수: **M2 완료 시점의 보드는 여전히 빈 채로 남는다** — 2026-08-15 결정이 적은 감수가 한 베팅 더 유지된다)
- 2026-08-18 **봇의 산출물을 「폼」에서 「읽을 수 있는 전략 코드」로 넓히는 것을 다음 베팅에서 검토한다.** QuantDinger 는 AI 가 Monaco 편집기에 Python 을 써 주고 사람이 **읽고 고치고 저장**한다 — 산출물이 코드라 버전이 남고 눈으로 확인된다. 우리 M2(#150)는 대화가 **폼을 채운다.** 함께 본 것: 저쪽은 편집기 안에 *"지표 스크립트는 플롯과 매수·매도 신호만 계산한다 — 포지션 크기·리스크·수수료는 실행 설정의 몫"* 이라고 **경계를 사람이 손대는 자리에 적어 뒀다.** 우리 「경로를 보여준다」 원칙과 같은 본능인데 우리는 문서에만 적었다 (지금 바꾸기 기각 — M2 의 완료 판정이 「조건이 폼에 그대로 보임」이라 판정 기준을 작업 중에 바꾸는 것이 된다)

- 2026-08-19 **API 키는 앱이 각 서비스의 `.env` 파일에 직접 쓴다** (#225 리드 결정). 배경: 키가 10개 서비스 ~15개 변수에 흩어져 있고 **MCP 서비스에는 DB 가 없다**(설계 결정 — `.env.example` 에 DB 변수 0개 실측). 그래서 「DB 에 저장」은 backend-service 한 곳에만 닿고 뉴스·공시·웹검색·리서치 LLM 은 여전히 파일을 본다. `.env` 쓰기는 **모든 서비스에 닿는 유일한 방식**이고, 로드맵의 「1차 배포 형태는 각자 자기 컴퓨터에 설치하는 로컬판」과 맞는다 (DB 암호화 저장 기각 — MCP 에 DB 를 주면 「MCP 는 DB·LLM 없음」을 뒤집고, 키를 서비스 간에 물어보게 하면 새 계약이 생기고 키가 네트워크를 탄다 / 읽기 전용 안내판 기각 — 「넣고 저장」이 안 된다 / **감수: 반영에 재기동이 필요하다** — #225 가 원한 「재기동 없이 반영」은 포기한다. **감수: 앱이 파일 쓰기 권한을 갖는다** — 그래서 이 화면은 로컬판 전용이고 호스팅 모드에서는 비활성이다)

- 2026-08-19 **M2 의 「값이 소스와 일치한다」는 샘플 소스로 성립한 것으로 본다** (#2 리드 판정). 키 없이 도는 샘플 소스(#217)로 64봉이 어댑터·DB 두 단계에서 일치하는 것을 `verify_sample_roundtrip.py` 가 잠갔다 (실소스 대기 기각 — 키가 언제 올지 모르는 것에 완료 판정을 매달면 M2 가 끝나지 않는다 / 감수: **실소스로는 아직 안 태웠다** — 어댑터 계약이 같으므로 키가 들어오면 같은 경로를 코드 변경 없이 다시 태운다). 같은 턴에 #150(봇 만들기)도 닫았다 — B0~B3 이 다 main 에 있고 남은 것은 리드가 직접 넣을 `ANTHROPIC_API_KEY` 하나라 추적할 작업이 아니다. **M2 터미널 골조 12/12.**
- 2026-08-19 **`.env` 를 쓸 때 백업 파일을 남기지 않는다** (#225 리드 판정). 되돌리기는 쉬워지지만 `.env.*` 는 gitignore 대상이라 **옛 키가 평문으로 디스크에 쌓인다** (백업 기각 — `.env` 는 사용자가 직접 고치는 파일이라 원본이 이미 사용자 손에 있고, 비밀값 사본을 늘리는 것이 되돌리기 편의보다 비싸다 / 감수: 잘못 쓴 값은 사용자가 직접 되돌린다 — 그래서 쓰기는 **표에 있는 변수 한 줄만** 갈고 주석·다른 변수·줄 순서는 손대지 않는다)
- 2026-08-20 **일봉은 조회 시점에 정규장으로 접는다 (A′)** ([#255](https://github.com/Danwoo/trading-lab/issues/255) 리드 결정 · 집행은 [#298](https://github.com/Danwoo/trading-lab/issues/298)). 토스 일봉이 종목마다 다른 창을 덮는다 — 표본 25종목 중 9종목(36%)이 08:01–20:00 을 덮고 005930 은 정규장 종가와 −1.86%/+4.04% 어긋났다. 백테스트가 **정규장에서 낼 수 없는 가격으로 체결**하는 것이라 차별화 축 1번(검증의 현실성)과 정면으로 부딪친다. 저장은 소스 일봉 그대로 두고 **물어볼 때** 분봉이 그 날의 정규장을 덮으면 접어서 답한다 (A 적재 시점 접기 기각 — 실제로 구현했다가 시계열이 날짜끼리 갈리고·장중 반쪽 캔들이 굳고·수능일이 안 닫혀 되돌렸다 / B 현상 유지 기각 — 아무것도 안 깨지지만 체결가가 계속 어긋난다 / C 분봉 전 구간 적재 후 접기 기각 — 1년치가 종목당 478페이지라 적재량이 크다). 순서는 **분봉 적재([#258](https://github.com/Danwoo/trading-lab/issues/258), 닫힘) 먼저, 그다음 A′** 다. **감수: 분봉이 없는 구간은 여전히 소스 값이라 한 응답 안에서 섞인다** — 그때 `mixed` 로 정직히 답한다. **감수: 수능일은 안 접는다** — `exchange_calendars` 4.13.2 가 2021년 이후 수능일의 1시간 지연을 모르는 것을 실측했고(2021-11-18·2023-11-16·2025-11-13 이 전부 09:00 로 나온다), 캘린더 값으로 접으면 15:30–16:30 의 진짜 정규장 거래가 잘린다. `EXTRA_CLOSURES` 와 같은 계통의 보정 목록으로 그 날들을 접기 대상에서 뺀다.
- 2026-08-23 회원가입=`operator` 배정 (`user` 유지+쓰기 가드 낮추기 기각 — 「쓰기는 operator 이상」이 모듈마다 갈라져 21개 라우트를 매번 판단해야 함 / 승인 절차 기각 — 호스팅 SaaS 는 현재 no-go 라 로컬 배포에선 혼자 승인하는 군더더기 / `user` 는 「초대받은 읽기전용 게스트」로 남음 — CONTEXT 의 「개인 워크스페이스+게스트 초대」 재해석과 정합 / 가입만으로 적재가 돌아 외부 API 호출량이 계정 수에 비례하는 것 감수 — 호스팅 열 때 재검토)
- 2026-08-25 **감사·운영 시각 컬럼을 `timestamptz` 로 옮긴다 (C)** ([#359](https://github.com/Danwoo/trading-lab/issues/359) 리드 결정. A「naive=UTC」·B「naive=KST」기각 — 뜻을 규약으로 못 박아도 DB 세션 tz 에 종속돼 배포마다 다시 어긋난다; 감수 — alembic 52컬럼 + Prisma 35곳 마이그레이션, 환경별 보정값 백필). 결과로 DB 세션 tz 를 UTC 로 코드에서 고정한다 — `@prisma/adapter-pg` 는 세션 tz 가 UTC 가 아니면 `timestamptz` 에도 틀린 인스턴트를 넣는다(실측). 시세·거래 업무 시각(`ts`·`entry_ts`·`exit_ts`·`nav_dt`)은 2026-07-30 「시장 현지 벽시계」 그대로.
- 2026-08-25 **CI 밖 리뷰·머지의 승인자는 GitHub App 이다** (리드 결정. 봇 사용자 계정 기각 — 이메일·2FA·PAT 만료를 사람이 관리해야 하고 public 복귀 때 「개인 계정으로 자기 승인」으로 읽힌다 / 승인 없이 라벨만 기각 — 「사람이 봤다」의 증거가 자기 신고 라벨뿐이 된다). App 은 `<이름>[bot]` 신원으로 Approve 해 ruleset 「승인 1」을 채우고, 타임라인에 사람 승인과 구분되어 남는다.
- 2026-08-29 **지휘자의 직접 머지를 허용한다 — deny 훅을 푼다** (리드 결정: 「중요하게 결정해야 하는 부분이면 머지를 안 하면 되고, 그런 케이스가 아니면 바로 머지를 해주셔야합니다」). **2026-08-09 「머지 주체는 경로로 가른다」의 집행 수단이던 `Bash(gh pr merge*)` deny 를 제거한다.** 그 결정이 훅에 기댄 이유는 지휘자와 리드가 같은 GitHub 계정을 쓰기 때문이었고, 그 사실은 지금도 그대로다 — **따라서 대가는 머지 주체 구분이 다시 흐려지는 것이다**(지휘자가 직접 민 머지는 리드가 누른 것과 같은 액터로 남는다. 워크플로가 민 것만 `app/…` 로 갈린다). **남는 기계 방어는 ruleset `main protection` 하나다** — PR 필수·승인 1건·required 체크 3종, `bypass_actors` 0건(2026-09-01 실조회). `agent: merged` 라벨은 2026-08-09 에 기각됐고 그것을 읽을 `merge-record` 잡은 이 레포에 **없다** — 완화책으로 세지 않는다. **감수: 지휘자가 「결정거리가 아니다」를 오판하면 그대로 머지된다. 그 판단에 기계 검사는 없다.**
- 2026-09-02 **임시 시세는 「못 읽을 값」으로 낸다** (Cycle 7 Q1 리드 결정). 종목 정보 패널이 실시간 계약 없이 **모든 종목에 같은 그럴듯한 값**(74,200원 ▲1,200)을 내던 것을 `—`·회색 막대 같은 **오독이 원리적으로 불가능한 표시**로 바꾼다 (그럴듯한 값 유지 기각 — 배지를 못 본 사람이 진짜로 읽고, 여러 종목을 훑기 전엔 같은 값인 줄 모른다 / **종목마다 다른 합성값 기각 — 가장 위험하다**, 그럴듯한데 종목마다 다르면 진짜와 구별할 단서가 사라진다). **감수: 화면이 「고장난 것」처럼 보일 수 있고, 실데이터가 붙었을 때의 모습을 미리 못 본다.** 근거: 발굴 F11.
- 2026-09-02 **합성 NAV 적재를 끈다 — 화면이 생길 때 켠다** (Cycle 7 Q2 리드 결정). producer 가 10초마다 쌓던 것(10일 35,342행)을 멈춘다. 읽는 화면이 없고, 네 계열이 **서로 독립 난수**라 원리적으로 검산이 안 된다 (파생 계열을 nav 에서 계산하도록 고쳐 두기 기각 — 지금 쓰지도 않는 것에 드는 일감이고 무한 적재는 그대로 / 그대로 두기 기각 — 화면을 붙이는 사람이 검산 안 되는 숫자를 물려받고 그때는 「화면 버그」로 보인다). **감수: 화면을 만들 때 볼 데이터가 없어 시드를 다시 만들어야 한다.** 근거: 발굴 F8·F9·F10, [#437](https://github.com/Danwoo/trading-lab/issues/437).
- 2026-09-02 **포트폴리오는 사람의 장부이자 봇의 기록이다 — 출처를 구분해 보인다** (Cycle 7 Q3 리드 결정). 같은 화면에 둘을 두되 각 행이 「사람이 적음」인지 「봇 #N 이 남김」인지 보인다 (사람 장부만 기각 / 봇 기록만 기각 — 수동 장부를 걷어내야 한다). **감수: 스키마·화면·검증이 모두 늘어난다** — 출처 컬럼, 화면의 구분 표시, 그리고 출처별로 다른 검증 규칙(사람 입력은 오타 방어, 봇 기록은 무결성).
- 2026-09-02 **스타터 템플릿 잔재를 지금 한 번에 걷는다** (Cycle 7 Q4 리드 결정). 화면의 옛 낱말(「Git ID」)과 **같은 일을 하는 두 방식이 공존하는 다섯 자리**를 전수 조사해 정본을 정하고 옮긴다 (지나가는 길에서만 걷기 기각 — 잔재가 오래 남고 본 사람이 또 판단해야 한다 / 사용자에게 보이는 것만 먼저 기각). **감수: 상당한 일감이고 그동안 제품 기능이 안 나간다. 다만 이후 모든 작업이 빨라진다.** 근거: 발굴 F16·F24·F25·B-34, [#442](https://github.com/Danwoo/trading-lab/issues/442).
- 2026-08-27 **베팅 넷을 동시에 세우되 Now 는 하나로 둔다** (리드 결정 — 「모든 베팅을 다 골라야 한다」). M2·M3 가 100% 닫히고 열린 이슈가 1건만 남아 사이클이 목표 없이 돌던 상태를 끝낸다. 마일스톤 M4(보드를 믿는다)·M5(봇을 굴린다)·M6(리서치 기둥)을 함께 세우고 순서를 굳히기 → M4 → M5 → M6 로 박았다 (넷을 동시에 Now 로 두는 안 기각 — 이 레포가 이미 「목표가 안 보인다」로 값을 치렀다; 감수 — M5·M6 착수가 늦어진다). 순서 근거: 믿을 수 없는 백테스트 위에 실행 엔진을 얹으면 봇이 왜 그렇게 굴렀는지 설명할 근거가 없다.
- 2026-08-27 **브랜치 이름은 `feature/<티켓번호>-<짧은-설명>`** (리드 결정. `fix-<이슈>-<에이전트>` 기각 — 벤더 슬러그가 자동화의 저자 판별에 얽혀 있었고, 그 판별은 커밋 신원으로 옮긴다). 이슈 없는 작업은 `chore/<설명>`, 목표층 문서만은 `docs/<주제>`. 정본은 `.work/orders/BRANCH-RULE.md`. 실측 둘을 함께 못박았다 — 이름은 처음에 맞게 판다(rename 이 열린 PR 을 닫는다), 머지된 브랜치에 다시 push 하지 않는다.

## 자율 사이클 진행 상황

<!-- 운영 규약(2026-08-19 리드 프롬프트, Cycle 0~10)의 상태 커서. 매 단계 종료 시 갱신 · 세션이 끊겨도 여기서 복구한다. -->

현재: **Cycle 8 완료 · 코드 PR 4건이 머지 불가로 쌓여 있다** (2026-09-03). Cycle 7 의 #434 를 닫는 PR 을 포함해
#448·#449·#450·#451 이 전부 `review: unable` 로 BLOCKED — cross-review 가 **Orca 런타임 없이는** 리뷰 워커를
못 세우고, 승인 리뷰가 없으면 ruleset 이 머지를 막는다. **리드가 Windows Orca 앱을 띄우는 것 외에 에이전트가
풀 수 있는 경로가 없다.**

**Cycle 5 결과**: 이슈 11건 중 **4건 닫힘**(#397·#398·#399·#407). 남은 7건은 M4/M5 소속이라 굳히기 뒤 차례다.

**굳히기에서 닫은 것 (2026-08-28~09-01)**: CI 재설계 A~D(#390·#392) · 감사 시각 timestamptz(#393) · vitest 병렬 상한 4(#411) · **문서 전용 PR 의 App 승인·자동 머지**(#414 — 승인자 부재로 문서 PR 이 영구 차단되던 구멍) · **지휘자 직접 머지 허용**(#421 — 리드 결정, 대가는 결정 로그에 적었다) · 의존성 상승 2건(#418·#419).

**굳히기에서 닫은 것 (2026-08-28)**: CI 재설계 A~D(#390·#392 — 워크플로 10→5, 체크 32→6) ·
감사 시각 timestamptz 전환(#393) · vitest 병렬 상한 4(#411 — 코어 수만큼 띄우던 것이 한 번에 3GB) ·
**문서 전용 PR 의 App 승인·자동 머지**(#414 — 승인자가 없어 문서 PR 이 영구 차단되던 구멍. 이 파일의 #408 이 그 첫 통과다) ·
Cycle 5 의 #397(#410)·#399(#409).

**한 사이클 = 페르소나로 제품을 직접 써 보며 마찰을 전수로 뽑아 이슈화하고, 해결해 머지까지.**
종전에는 「이슈 하나 해결 = 한 사이클」로 잡아 속도도 발굴량도 부족했다 (리드 지시 2026-08-20).

| 사이클 | 발굴 | 상태 |
|---|---|---|
| 1 | 15건 (#281~#295) | 완료 — 14건 머지, 오탐 8건은 실측으로 확인하고 버렸다 |
| 2 | 9건 (3 페르소나) | 완료 — 9건 전부 수정 |
| 3 | 5건 (#329~#333) | 완료 — 5건 전부 머지 (#334~#338) |
| 4 | **19건** (#341~#356 · #359~#361) | 닫힘 17건 · 남은 2건(#356 PR 판정 중, #359 결정 C → 마이그레이션 계획 중) |
| 5 | **25건** (W4 백테스트 16 · W5 4 · S2×W1 3 · 회귀 2) | 이슈 11건 발행(#397~#407) → **닫힘 4**(#397·#398·#399·#407) · 남은 7건은 M4/M5 소속. 통과 확인 17건은 findings 에 별도 기록 |
| 6 | **9건** (W2 종목·W6 봇 대화·W7 리서치·S4 390px·S1 첫 진입·게스트 403·회귀) | 이슈 **4건**으로 묶어 발행 → **4건 전부 닫힘** — [#422](https://github.com/Danwoo/trading-lab/issues/422) 종목 검색 정렬 · [#423](https://github.com/Danwoo/trading-lab/issues/423) 스트리밍 실패 사유 3건 · [#424](https://github.com/Danwoo/trading-lab/issues/424) 화면 상태 불일치 2건 · [#425](https://github.com/Danwoo/trading-lab/issues/425) 390px 차트. F2 는 #402 에 원인 근거 첨부, F3 는 오독으로 정정. **통과 확인 6건**(게스트 403·operator 첫 진입·390px CLS 0·격자 실주행 25칸·봇 서비스 부재 배너·/admin MDI) |
| 7 | **71건** (포트폴리오·NAV·호가/종목정보·관심목록·스케줄러 + 파일·리서치문서·설정키·마이페이지·접근성·경계값·동시성) | **[사용자] 19건 · [개발자] 20건** — 리드 지시로 체감 시간(초 단위)과 개발자 렌즈(기능 부재·레거시·코드 패턴)를 오더에 넣은 결과다. 이슈 **13건**: [#433](https://github.com/Danwoo/trading-lab/issues/433) 보안 경계(critical) · [#434](https://github.com/Danwoo/trading-lab/issues/434) 조용한 데이터 변형 · [#435](https://github.com/Danwoo/trading-lab/issues/435) 틀린 처방 · [#436](https://github.com/Danwoo/trading-lab/issues/436) 파일 모듈 · [#437](https://github.com/Danwoo/trading-lab/issues/437) NAV · [#438](https://github.com/Danwoo/trading-lab/issues/438) 체감 시간 · [#439](https://github.com/Danwoo/trading-lab/issues/439) 스케줄러 · [#440](https://github.com/Danwoo/trading-lab/issues/440) 실패가 성공처럼 · [#441](https://github.com/Danwoo/trading-lab/issues/441) 기능 부재 · [#442](https://github.com/Danwoo/trading-lab/issues/442) 템플릿 잔재 · [#443](https://github.com/Danwoo/trading-lab/issues/443) 접근성 · [#445](https://github.com/Danwoo/trading-lab/issues/445) 오해시키는 화면 · [#446](https://github.com/Danwoo/trading-lab/issues/446) 동시성. **리드 결정 4건**은 결정 로그 2026-09-02 |
| 8 | **12건** (띄우기 7 · 화면 5) | 이슈 **2건**: [#452](https://github.com/Danwoo/trading-lab/issues/452) 띄우기 7건 · [#453](https://github.com/Danwoo/trading-lab/issues/453) 화면 3건. **오탐 6건을 확인으로 취소**한 것이 이 사이클의 주된 산출이다. #404 로 보낸 것 1건 |

**Cycle 4 부터 실 DB 로 검증한다.** 도커가 죽어(dockerd 미기동) 세 사이클 내내 「검증 없음」이
쌓였는데, 유저스페이스 wheel(`pgserver`)로 우회했다 — PostgreSQL 16.2 를 root·도커 없이
127.0.0.1:55440 에 띄운다. PGDATA 는 `~/pgdata-trading-lab`, 레시피는 `.work/local-stack-no-docker.md`.

대기 중인 결정: 없음
**Cycle 4 가 실 DB 로 잡은 것 — 이전 세 사이클엔 없던 종류다.**
보안 3건: 이메일 인증 없이 계정이 만들어짐(#343) · `.env` 를 쓰는 API 가 최하위 권한에 열림(#344) ·
권한을 회수해도 세션이 5분 더 통함(#354). 정합 2건: 격자가 거래 0건 16칸을 「최고 성적」으로 칠함(#349) ·
같은 컬럼을 통합 앱과 Prisma 가 9시간 다르게 읽음(#359).

**2026-08-24 진단 (리드 지적으로 실측).** 세션이 4회 끊긴 원인은 워커를 Claude Code 서브에이전트로 띄운 것이었다 —
Orca 가 모르는 자식이라 자원 회수가 안 되고, 커널이 dbus 를 정리하면 Orca 가 넘어져 그 자식인 지휘자·워커·스택이
함께 내려간다(`journalctl --user -u orca-runtime` 4회 동일 시퀀스). 시작한 워커 122개 중 53개(43%)가 산출 없이
사라졌다. 느린 이유도 같은 뿌리다 — 머지 코드의 76% 가 그물·CI 였고(제품 2,342줄 vs 그물 7,569줄), 리뷰 36% 가
2라운드였다. Orca 관리 워크트리·레포 등록은 **0개** — 규약 `orchestration.md §6` 의 `worktree create --agent`
경로를 한 번도 쓰지 않았다.

**리드 결정 2026-08-24 — 워커 작업 공간 = Orca 워크트리** (규약 §6.2 미결 확정; 별도 클론·Orca 버리기 기각 —
Projects 패널 가시성과 Orca 의 자원 계정·정리를 취함). #364·#370 라운드2 를 그 경로로 띄워 검증 중.
오더는 결함 등급(보안·정합 / UX)에 맞춰 무게를 가른다.

**Cycle 8 의 결론 — 마찰이 화면에서 기동으로 옮겨갔다 (2026-09-03).** 실험대를 시드 계정으로 실제로 눌러 본
결과 결함이 거의 없었다: 격자 폼의 거부 경로 다섯 가지가 **전부 사유를 말하고**(「시작 자금을 0보다 크게
적으세요 — 성과를 재는 분모입니다」·「종목 마스터에 없는 종목입니다: KOSPI ZZZZZZ」), 실행 중 버튼은
「돌리는 중…」으로 잠기고(실행 747ms), 적재 버튼은 비활성 라벨로 안내하고(`종목을 고르면 적재합니다`),
폭 1440→600px 전 구간에서 가로 스크롤이 0이고, 콘솔 오류는 0건이다. 반면 **제품을 띄우는 데 세 번을 다시
띄워야 했다** — 2주 전 죽은 컨테이너 하나가 스택 전체를 막았고(#452 A-1), 실패 문구는 그 문제를 아는 레포
자체 도구(`scripts/bootstrap_local_env.py`)를 가리키지 않았다(A-3).

**같은 사이클에서 오탐 6건을 만들었다가 확인으로 전부 취소했다.** 원인은 하나다 — 출력을 자른 채
(`.slice(0,12)` · `head` · `force:true` · 823ms 첫 표본) 「없다」고 적었다. 부재는 전수 주장이라 자른 표본으로는
성립하지 않는데, **자른 사실이 출력에 안 남아 스스로도 못 알아챈다.** 발굴량을 늘리라는 지시가 있을 때 특히
위험한 자리다.

다음: Orca 복구 → PR 4건 머지 → Cycle 9

## 교훈

- 로컬 실행이 안 되는 상태로 "완료"를 선언하지 않는다 — 통합 실행 0회가 차단막 6개를 숨겼다(2026-07 실측).
- 브라우저로 열어보지 않은 화면은 "된다"고 말하지 않는다 — curl 로는 CSP 자기 차단(전 화면 iframe 백지)을 잡을 수 없었다.
- 제품 정의 없이 기술부터 쌓으면 화면이 섞인다 — 개발 관리 화면과 투자 화면이 한 메뉴에 있었고 아무도 이상하게 여기지 않았다.
- "무료로는 안 된다"를 조사 없이 단정하지 않는다 — 토스증권 오픈API 부재를 전제로 질문을 짰다가 뒤집혔다.
- **실패가 조용히 성공처럼 보이는 자리를 의심한다.** 2026-08-06 하루에 같은 클래스가 다섯 번 나왔다 — 권한 부족 403 이 「판정 없음」으로 삼켜져 26분을 태웠고(`|| true`), 워크트리 정리 실패가 「이미 없음」으로 조용히 성공했고, 그물이 검사 대상을 절반만 보면서 초록이었다. 조회·정리·검사가 **실패했는지 아닌지를 결과와 구분해서** 드러내야 한다.
- **증상 하나로 원인을 단정하지 않는다 — 로그를 먼저 가져온다.** 같은 날 CI 리뷰 장애의 원인을 세 번 짐작으로 말했고(폴링 간격·프라이머·페이지네이션) 셋 다 틀렸다. 러너 로그 첫 줄에 30초마다 403 이 찍혀 있었다.
- **새 화면을 옆 화면에서 복사하지 않는다 — 그 자리의 정본 문서를 먼저 연다.** 2026-08-09 실측: 디자인 시스템(색 토큰 두 벌·타이포 스케일·선 위계 4단·상태 규칙)이 그날 서 있었는데, 새 패널을 기존 패널에서 복사해 쓰면서 **폐기 대상 토큰**으로 그렸다. 겹치는 이름은 `ink-muted` 하나였고 라이트 모드는 아예 없었다. 옆 파일은 「지금 맞는 것」이 아니라 「그때 맞았던 것」이다.
- **표식을 늘리는 것은 방어가 아니다.** 사건 하나에 규칙·CI 잡·이슈·테스트 케이스를 하나씩 붙이면 표식 밖은 여전히 뚫리고, 많아진 표식이 무엇이 중요한지를 가린다 — 같은 습관이 danger-guard 표(6,870케이스)·CI 잡(36개)·이슈(39건)에서 동시에 드러났다.
- **산출물이 어디 저장되는지 확인한다 — 만드는 것과 남기는 것은 다르다.** 2026-08-09 화면 시안 30개를 하루 종일 만들었는데 저장 위치가 `.gitignore` 안이라 레포에 **하나도 없었다**(`git ls-files` 0건). 결정 문서만 남고 그 문서가 설명하는 화면은 사라질 뻔했다. 도구가 기본으로 쓰는 작업 디렉터리는 **공유 대상이 아닐 수 있다**.
- **대기열이 안 줄면 일꾼이 살아 있는지부터 본다 — 큐가 아니라 소비자를 의심한다.** 2026-08-22 실측: PR 11건이 판정 없이 쌓여 「리뷰가 느리다」로 읽혔는데, 원인은 리뷰가 아니라 **자체 호스팅 러너 2대가 전부 오프라인**인 것이었다(WSL 재시작 때 죽은 채 방치). 교차리뷰는 그 러너에만 배정되므로(`runs-on: [self-hosted, …]`) 잡은 영원히 `queued` 로 남고, **`queued` 는 실패로 보이지 않는다**. `gh api …/actions/runners` 한 번이면 25초에 끝날 일을 대기로 태웠다. 「조용히 실패하는 자리」의 한 종류다 — 이번엔 그물이 아니라 그물을 치는 사람이 없었다.
- **막힌 의존을 우회할 수 있는지 먼저 센다 — 없다고 단정하기 전에 유저스페이스를 본다.** 같은 날 도커가 죽어(dockerd 미기동) 세 사이클 내내 DB 검증이 「검증 없음」으로 쌓였고, `sudo` 도 막혀 시스템 패키지 설치가 안 됐다. 그런데 PostgreSQL 은 **wheel 로 배포된다**(`pgserver`) — root 도 도커도 없이 16.2 가 떴다. 되돌릴 수 없어 보이는 차단이 실은 배포 형태 하나만 바꾸면 되는 것이었다.

- **가짜 데이터 위에서 일관성을 손으로 맞추면 수렴하지 않는다.** 같은 날 화면 시안을 페르소나로 4회 검증했는데, 4차가 잡은 지적 중 6건이 「지어낸 숫자끼리 안 맞음」이었고 그중 하나(`유효 종목 3.5`)는 **어떤 분포로도 나올 수 없는 값**이었다 — 상한이 2.837인데 그 위에 금액 계산 전체가 서 있었다. 시안으로 판정할 수 있는 것은 「무엇을 어떤 순서로 보여줄 것인가」까지이고, **숫자의 타당성은 실측이 생긴 뒤에 본다.**
