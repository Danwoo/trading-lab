"""저장소가 시각 컬럼을 문자열로 낼 때 쓰는 포맷 하나 (#359).

## 왜 상수 하나인가

감사·운영 컬럼이 `timestamptz` 로 옮겨진 뒤(alembic `0019`), 종전의
`to_char(c, 'YYYY-MM-DD HH24:MI:SS')` 는 **오프셋 표식 없이 세션 벽시계**를 낸다. 세션은 UTC 로
고정돼 있으므로 자릿수는 UTC 인데, 표식이 없으니 브라우저의 `new Date(value)` 는 그것을
**로컬 시각**으로 읽는다 — KST 사용자에게 9시간 과거로 보인다.

그래서 오프셋을 붙인다. `OF` 가 아니라 `TZH:TZM` 인 이유: `OF` 는 `+00` 을 내는데 V8 은
`"2026-08-23T02:24:00+00"` 을 `Invalid Date` 로 본다(`T` 구분자 + 2자리 오프셋 조합). `TZH:TZM`
은 `+00:00` 을 내고 이건 `T` 구분자와 함께 인스턴트로 읽힌다.

한 자리에 모아 두는 이유는 22곳이 각자 문자열 리터럴을 들고 있으면 갈리기 때문이다 — 실제로
이 레포에서 `nav_dt` 만 `T` 구분자를 쓰고 나머지는 공백을 써서 같은 API 계열의 응답 모양이
둘로 갈려 있었다.
"""

# 인스턴트를 표식과 함께 낸다 — 브라우저가 사용자 tz 로 그린다. 시장 시각 컬럼에는 쓰지 않는다.
DT_ISO_TZ = 'YYYY-MM-DD"T"HH24:MI:SSTZH:TZM'
