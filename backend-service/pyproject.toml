[project]
name = "app"
version = "0.1.0"
license = "MIT"
requires-python = ">=3.12,<3.13"
dependencies = [
    # 웹 프레임워크
    "dependency-injector~=4.49.1",
    "httpx~=0.28.1",
    "tenacity~=9.1.4",
    "python-multipart~=0.0.32",
    "uvicorn[standard]~=0.52.1",
    "fastapi~=0.141.1",
    "pydantic-settings~=2.15.0",
    "pyjwt~=2.13.0",
    # 데이터베이스
    "sqlalchemy~=2.0.52",
    "psycopg[binary]~=3.3.4",
    "alembic[tz]~=1.19.1",
    "exchange-calendars>=4.5",
]

[dependency-groups]
main = [
    # file 모듈 — SFTP 업로드·이미지 프리뷰
    "pillow~=12.3.0",
    "asyncssh~=2.24.0",
    # chat·report 모듈 — LangGraph 멀티 MCP 에이전트 + APScheduler
    "mcp>=1.27,<2",
    "langchain==1.3.15",
    "langgraph==1.2.11",
    "langchain-core==1.5.4",
    "langchain-openai==1.4.3",
    "langchain-mcp-adapters==0.3.2",
    "apscheduler>=3.10,<4",
]
dev = ["ruff~=0.16.2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
default-groups = ["main", "dev"]
package = false

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["F", "I", "FAST", "W", "E", "B", "UP"]
ignore = [
    "B008",    # FastAPI Depends 기본값 함수 호출 허용
    "E501",    # 라인 길이 88자 초과 무시 (black 기준)
    "E402",    # 동적 import 허용 (FastAPI lazy loading)
    "FAST002", # 선택적 의존성 타입 어노테이션 무시
]
