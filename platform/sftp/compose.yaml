services:
  sftp:
    image: atmoz/sftp:debian
    container_name: atmoz-sftp-server
    restart: always
    ports:
      - 2022:22
    volumes:
      - sftp_data:/home
    command: |
      admin:admin:::upload

volumes:
  sftp_data:
    name: atmoz_sftp_data
