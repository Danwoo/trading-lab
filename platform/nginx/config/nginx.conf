worker_processes auto;
worker_cpu_affinity auto;
worker_rlimit_nofile 65535;

events {
    worker_connections 1024;
    multi_accept on;
}

http {
    access_log off;
    client_max_body_size 10M;
    proxy_send_timeout 300s;
    proxy_read_timeout 300s;
    proxy_connect_timeout 120s;

    upstream web {
        server fullstack-web:3000;
        keepalive 512;
    }

    # /file-service/ 경로는 그대로 두고 대상만 통합 앱으로 바꾼다 —
    # frontend fileService.ts 가 이 경로 세그먼트를 쓰고, 통합 앱의 라우트는 여전히 /file/... 이다.
    upstream file {
        server fullstack-backend:8000;
        keepalive 128;
    }

    map $http_upgrade $connection_upgrade {
        default upgrade;
        '' close;
    }

    server {
        listen 80;
        server_name _;

        location / {
            include /etc/nginx/conf.d/proxy_params_http.conf;
            proxy_pass http://web/;
        }

        location /file-service/ {
            include /etc/nginx/conf.d/proxy_params_http.conf;
            proxy_pass http://file/;

            client_max_body_size 2g;
            proxy_read_timeout 900s;
            proxy_send_timeout 900s;
            proxy_request_buffering off;
            client_body_timeout 900s;
            send_timeout 900s;
            proxy_set_header Accept-Encoding "";
        }
    }
}
