"""계좌번호·카드번호 등 자유텍스트에 섞인 민감 식별자를 마스킹 (노출 전 결정론적 전처리).

브로커리지/포트폴리오 데이터(계좌·거래·주문·활동 라인)는 챗 에이전트(LLM)·리포트로 흘러가므로,
계좌번호 같은 식별자가 그대로 노출되지 않도록 이 모듈이 **데이터 소스(portfolio-mcp)에서** 마스킹한다.
- **계좌/카드 식별자 전용** — 그 외 개인정보(주민번호 등)는 LiteLLM 게이트웨이 PiiMaskGuard 가 LLM 입력단에서 담당(역할분담).
  식별자는 게이트웨이(LLM 경계)가 아니라 **source 에서** 가려, 서비스 밖(전송·로그·리포트·LLM)으로 아예 안 내보낸다.
- 짧은 라벨/계좌번호 대상이라 오탐을 줄이려 **고정 포맷만** 매칭한다(브로커 계좌번호·카드번호 형태).
  high-entropy 추정 마스킹은 하지 않는다.
- 계좌번호는 가운데 자릿수만 가리고 앞뒤 일부는 남겨 식별성을 유지한다(전액 마스킹 아님).
- 종목명·티커·금액(기능상 필요) 은 건드리지 않는다 (계좌·카드 식별자만 대상).
"""

import re

_MASK = "[계좌번호 일부 가려짐]"


def _mask_account_no(m: re.Match) -> str:
    """계좌번호/카드번호의 가운데를 가리고 앞 2 · 뒤 2 그룹만 남긴다 (구분자 유지)."""
    raw = m.group(0)
    digit_groups = re.findall(r"\d+", raw)
    if len(digit_groups) < 3:
        return _MASK
    return f"{digit_groups[0]}-{_MASK}-{digit_groups[-1]}"


# YYYY-MM-DD 날짜 — 계좌가 아니라 기능 데이터라 마스킹 대상에서 뺀다.
# 한글이 \w 라 `\b` 가 '2024-05-15만기' 에서 안 생기므로 뒤 경계는 `(?!\d)` 로 잡는다.
_DATE_AHEAD = r"\d{4}-\d{2}-\d{2}(?!\d)"

# 라벨 경계 — 영문 라벨은 `\b` 로 토큰 분리, 한글 라벨은 인접이 한글일 때 `\b` 가 안 생기므로
# (한글이 \w → '내계좌번호'·'계좌번호는' 이 경계 미형성) 경계 없이 매칭한다. 앞 접두('내…')는 흡수돼
# 값만 마스킹되고, 뒤 조사(는/를/…)는 아래 kv 패턴의 `_JOSA` 가 라벨과 구분자 사이에서 삼킨다.
_LABEL_EN = r"\b(?:account[_-]?no|account[_-]?number)\b"
_LABEL_KO = r"(?:계좌번호|카드번호)"
_LABEL = rf"(?i)(?:{_LABEL_EN}|{_LABEL_KO})"

# 한글 라벨 뒤 조사 — 라벨과 구분자 사이에서 선택적으로 흡수한다. 뒤 공백은 소비하지 않아
# 공백 구분자 분기(`\s++`)가 그대로 성립한다. '계좌번호는 5012…' 처럼 라벨 직후 조사가 흔하다.
# 조사는 **열거**한다(`[가-힣]+` 아님) — 임의 한글 런을 흡수하면 '계좌번호안내 005930' 의 종목코드까지
# 오탐하므로, 정확 매칭되는 조사만 흡수하고 값 게이팅(숫자형)이 오탐을 한 번 더 막는다.
_JOSA = r"(?:은|는|이|가|을|를|의|로|으로|와|과|에|에서|에는|부터|까지|마다|만|도)?"

# 값 조각의 끝 경계 — 조각은 숫자·하이픈으로 더 이어지지 않고, 글자나 자릿수 구분자가 붙지 않는다.
# 이게 없으면 '300주'·'1,000,000원'·'2024.05.15' 의 앞부분만 삼켜 잔해('주 보유', ',000,000원')를 남긴다.
_FRAGMENT_END = r"(?![\d-])(?![,.]\d)(?![^\W\d_])"

# 공백으로 쪼개진 값('5012 01 2345678').
# **첫 조각만** 9자 이하로 제한한다(`{1,8}`) — 이 상한이 "값이 실제로 쪼개졌는가"를 판별하는 장치다.
# 완성된 계좌번호(13자리·'5012-01-2345678' = 15자 런)는 첫 조각이 될 수 없어 이 분기가 아예 안 걸리고,
# 뒤따르는 티커·수량은 그대로 남는다. 첫 조각의 상한을 풀면 완성된 계좌가 '첫 조각'이 되어
# 뒤 토큰(005930 삼성전자 …)까지 반복부가 삼킨다 — 그래서 상한 유지는 과잉 마스킹 방어다.
# 반면 **이어지는 조각에는 상한을 두지 않는다**(`[\d-]*`) — 10자리 이상 조각 하나(`계좌번호 5012 0123456789`)가
# 상한 바로 바깥으로 빠져나가 값 전체가 미마스킹되던 누출을 막는다(#59). 첫 조각이 이미 쪼개짐을 증명했으므로
# 뒤 조각의 길이는 계좌 여부 판별에 쓸모가 없고, 정상 포맷 보호는 길이가 아니라 `_FRAGMENT_END` 가 한다
# (금액 '1,200,000'·수량 '300주'·글자 인접은 끝 모양에서 걸러진다).
# 단 `_FRAGMENT_END` 가 못 막는 축이 하나 있다 — **하이픈 날짜**(`2024-05-15`)는 순수 숫자·하이픈이라
# 끝 모양으로 안 걸러지고, 상한을 푼 뒤엔 조각으로 흡수돼 기능 데이터가 파괴된다(실측). 그래서 이어지는
# 조각 앞에 `(?!_DATE_AHEAD)` 를 둔다 — 아래 bare 하이픈 계좌 패턴이 이미 쓰는 것과 같은 규칙
# ("날짜 모양이 계좌 모양을 이긴다")이라 모듈 전체가 일관된다. 계좌 마스킹에는 영향이 없다
# (반복부는 날짜 앞에서 멈출 뿐, 이미 성립한 조각들은 그대로 가려진다).
# 개행도 넘지 않는다(`[^\S\n]`).
_VALUE_FRAGMENTED = rf"\d[\d-]{{1,8}}{_FRAGMENT_END}(?:[^\S\n]+(?!{_DATE_AHEAD})\d[\d-]*{_FRAGMENT_END})+"

# 공백 구분자 분기의 오탐 방지 — 쪼개진 값도 합쳐 6자리+ 일 때만 계좌로 본다.
_MIN_6_DIGITS = r"(?=(?:[^\S\n]*[\d-]){6})"

# 공백 구분자 bare 값 분기의 종목코드 가드 — **정확히 6자리 순수 숫자**(KRX 종목코드형, 005930)는
# 계좌 bare 값으로 보지 않는다. '계좌번호와 005930'(접속 조사)·'계좌번호 005930' 의 종목코드를
# 계좌값으로 오탐·파괴하던 회귀를 막는다(#99). `\d{6}` 뒤에 숫자·하이픈이 안 이어질 때만(=정확히 6자리)
# 거부하므로, 실계좌(13자리·하이픈 구분)는 6자리 초과라 그대로 마스킹된다. 라벨+구분자(:/=/：) 경로는
# 명시 라벨:값이라 이 가드를 두지 않는다(공백 분기만).
_NOT_TICKER6 = r"(?!\d{6}(?![\d-]))"

# 매치를 마스킹. 순서 주의 — 라벨+값(kv) 전체를 먼저 가린 뒤 남은 bare 번호를 부분 마스킹한다
# (반대로 하면 부분 마스킹된 결과를 kv 패턴이 다시 잘라 토큰이 깨진다).
_PATTERNS: list[tuple[re.Pattern, object]] = [
    # 'account_no: ...' / '계좌번호 5012012345678' 형태의 라벨+값 — 값 전체 마스킹 (먼저).
    # 구분자는 :/=/전각 콜론(：) 뿐 아니라 공백도 받는다(공백일 땐 값이 6자리+ 숫자형일 때만 — 오탐 방지).
    # 라벨 뒤 `\s` 는 possessive — 긴 공백 런에서 뒤 조건이 실패할 때의 재시도(2차식 백트래킹)를 끊는다.
    (
        re.compile(
            _LABEL + _JOSA + r"(?:"
            rf"\s*+[:=：]\s*+(?:{_VALUE_FRAGMENTED}|\S+)"
            rf"|\s++(?:{_MIN_6_DIGITS}{_VALUE_FRAGMENTED}|{_NOT_TICKER6}\d[\d-]{{5,}})"
            r")"
        ),
        _MASK,
    ),
    # 카드번호 (16자리, 4-4-4-4 / 공백 / 연속) — 앞·뒤 그룹까지 **전액** 마스킹.
    # 계좌처럼 앞2·뒤2 그룹을 남기면 '카드5012 0123 4567 8901' 이 '카드5012-…-8901' 이 되어 16자 중 8자(앞4·뒤4)가
    # 샌다(#52). 카드번호는 앞4+뒤4 만으로도 식별성이 커 계좌(부분 마스킹)와 달리 전액 가린다 → 마스크 문자열(_MASK)로 치환.
    # 한글 인접('카드5012 …') 에서 `\b` 가 안 생기므로 자릿수 경계는 bare 계좌 패턴과 동일하게 lookaround 로 강제한다
    # (긴 숫자런 안쪽 오탐 방지 성질은 `(?!\d)` 가 그대로 보존).
    (re.compile(r"(?<!\d)\d{4}[ -]?\d{4}[ -]?\d{4}[ -]?\d{4}(?!\d)"), _MASK),
    # 브로커 계좌번호 (3~4 그룹, 하이픈 구분, 예: 123-45-678901 / 5012-01-2345678) — YYYY-MM-DD 날짜 형태는 제외.
    # 한글 인접('계좌5012-01-2345678') 에서 `\b` 가 안 생기므로 자릿수 경계는 lookaround 로 강제한다.
    (
        re.compile(rf"(?<!\d)(?!{_DATE_AHEAD})\d{{2,6}}-\d{{2,6}}-\d{{2,8}}(?:-\d{{1,8}})?(?!\d)"),
        _mask_account_no,
    ),
]


def redact_secrets(text: str | None) -> str:
    """text 에 섞인 계좌번호/카드번호 등 식별자를 마스킹해 반환. None/빈 문자열은 빈 문자열로."""
    if not text:
        return text or ""
    for pat, repl in _PATTERNS:
        text = pat.sub(repl, text)
    return text
